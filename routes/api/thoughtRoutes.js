const router = require("express").Router();

// get all toughts
// get a single though by _id
// post new thought and add thought _id to the user's thought's array
// put update a thought by it's _id
// delete to remove thought by _id

// /:thoughtId/reactions
// post create reaction in single thought's reaction array
// delete reaction by reactionId

module.exports = router;
